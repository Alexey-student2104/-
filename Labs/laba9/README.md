**Лабораторная работа №9 — CRUD для приложения отслеживания курсов валют c SQLite базой данных**

---

# **1. Цель работы**

В рамках данной лабораторной работы было разработано веб-приложение для отслеживания курсов валют с использованием фреймворка Flask и базы данных SQLite. Основные цели работы включали:

* Реализацию полного цикла CRUD-операций (Create, Read, Update, Delete) для сущностей приложения
* Освоение работы с SQLite базой данных в файловом режиме для сохранения данных между сеансами
* Понимание принципов использования первичных и внешних ключей для создания связей между таблицами
* Применение архитектуры MVC с четким разделением ответственности между компонентами
* Создание системы подписок пользователей на валюты с отображением персональных отслеживаемых курсов
* Реализацию полноценной системы маршрутизации с обработкой GET и POST запросов
* Написание модульных тестов с использованием unittest.mock для проверки функциональности
* Научиться тестировать функционал на примере сущностей currency и user с использованием unittest.mock.

---

# **2. Описание моделей, их свойств и связей**

## **2.1. User**

|   Поле   |   Тип   |  Описание         |
| -------- | ------- | ----------------- |
|  `id`    |   int   |  Первичный ключ   |
|  `name`  |   str   |  Имя пользователя |
|`password`|   int   |    Хеш пароля     |
|`is_admin`| boolean |Флаг администратора|

Валидация:

* ID > 0
* Имя ≥ 2 символов

**Связь:**
User (1) — (N) UserCurrency

---

## **2.2. Currency**

| Поле        | Тип   | Описание                   |
| ----------- | ----- | -------------------------- |
| `id`        | int   | Primary Key                |
| `num_code`  | str   | Числовой код валюты        |
| `char_code` | str   | Символьный код (USD, EUR…) |
| `name`      | str   | Название валюты            |
| `value`     | float | Текущий курс               |
| `nominal`   | int   | Номинал                    |

Валидация:

* курс > 0
* номинал > 0
* символьный код длиной 2–5 символов

---

## **2.3. UserCurrency**

| Поле        | Тип   | Описание                   |
| ----------- | ----- | -------------------------- |
| `id`        | int   | Primary Key                |
|  `user_id`  | int   | Числовой код валюты        |
|`currency_id`| int   | Символьный код (USD, EUR…) |

Таблица связи **многие-ко-многим**.

```sql
CREATE TABLE user (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,
    password TEXT NOT NULL,
    is_admin BOOLEAN DEFAULT FALSE
);

CREATE TABLE currency (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    num_code TEXT NOT NULL,
    char_code TEXT NOT NULL,
    name TEXT NOT NULL,
    value REAL NOT NULL,
    nominal INTEGER NOT NULL
);

CREATE TABLE user_currency (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    currency_id INTEGER NOT NULL,
    FOREIGN KEY(user_id) REFERENCES user(id),
    FOREIGN KEY(currency_id) REFERENCES currency(id)
);
```

---

# **3. Структура проекта с назначением файлов**

```
laba9/
│
├── app.py                          # Основной файл приложения
├── requirements.txt                # Зависимости проекта
├── currencies.db                   # Файл базы данных SQLite
├── test_app_complete.py            # Модульные тесты
├── view_database.py                # Утилита просмотра БД
│
├── templates/                      # HTML шаблоны
│   ├── base.html                   # Базовый шаблон
│   ├── index.html                  # Главная страница
│   ├── author.html                 # Информация об авторе
│   ├── users.html                  # Список пользователей
│   ├── user.html                   # Профиль пользователя
│   ├── currencies.html             # Таблица валют
│   ├── login.html                  # Страница входа
│   ├── register.html               # Страница регистрации
│   ├── admin.html                  # Административная панель
│   ├── subscriptions.html          # Управление подписками
│   ├── user_subscriptions_admin.html  # Подписки пользователей (админ)
│   └── subscriptions_stats.html    # Статистика подписок
│
└── static/                         # Статические файлы
    └── style.css                   # CSS стили
```

---

# **4. Реализация CRUD с примерами SQL-запросов**

## **4.1. CREATE**

```python
# Python код
new_currency = Currency(
    num_code="840",
    char_code="USD",
    name="Доллар США",
    value=90.5,
    nominal=1
)
db.session.add(new_currency)
db.session.commit()

# Эквивалентный SQL
# INSERT INTO currency (num_code, char_code, name, value, nominal)
# VALUES ('840', 'USD', 'Доллар США', 90.5, 1);
```

---

## **4.2. READ**

Все валюты:

```python
currencies = Currency.query.all()

# SQL: SELECT * FROM currency;
```

Получение пользователя по имени:

```python
user = User.query.filter_by(username='Питон').first()

# SQL: SELECT * FROM user WHERE username = 'Питон' LIMIT 1;
```

Получение подписок пользователя:

```python
subscriptions = UserCurrency.query.filter_by(user_id=user_id).all()

# SQL: SELECT * FROM user_currency WHERE user_id = ?;
```

---

## **4.3. UPDATE**

```python
currency = Currency.query.filter_by(char_code='USD').first()
currency.value = 91.2
db.session.commit()

# SQL: UPDATE currency SET value = 91.2 WHERE char_code = 'USD';
```

---

## **4.4. DELETE**

Удаление валюты:

```python
currency = Currency.query.get(currency_id)
db.session.delete(currency)
db.session.commit()

# SQL: DELETE FROM currency WHERE id = ?;
```

Удаление пользователя:

```python
user = User.query.get(user_id)
db.session.delete(user)
db.session.commit()
```

---

# **5. Скриншоты работы приложения**

👇 *Вставь сюда изображения своих экранов*.

### **5.1. Главная страница ( / )**

![main](https://github.com/user-attachments/assets/927d5655-e620-421f-aa40-acace98dfc76)

### **5.2. Таблица валют ( /currencies )**

![tab_c](https://github.com/user-attachments/assets/b1c105c2-e2bd-47e8-bcf3-60a6d708c1aa)

### **5.3. Профиль пользователя ( /user?id=1 )**

![ex_prof](https://github.com/user-attachments/assets/4b395661-b8d3-4d1f-b462-548f3f477e62)

### **5.4. Пример Баз данных:*
![bd1](https://github.com/user-attachments/assets/2b48240c-d90e-4efb-8b1c-85fe77ac7ded)
![bd2](https://github.com/user-attachments/assets/ab09226c-e5f1-4647-8dd2-9d213cdd2df1)
![bd3](https://github.com/user-attachments/assets/9ae7e8d7-bb5c-4974-a9e8-baad9250d49c)

# **6. Примеры тестов с unittest.mock и результаты их выполнения**

## Тест хеширования пароля:

```python
def test_hash_password(self):
    """Проверка корректности хеширования пароля"""
    password = "PythonCool"
    hashed = hash_password(password)
    self.assertEqual(len(hashed), 64)  # SHA-256 дает 64 символа
    self.assertEqual(hashed, hash_password(password))  # Воспроизводимость
```

---

## Тест модели User

```python
def test_user_model_creation(self):
    """Проверка создания пользователя"""
    with self.app.app_context():
        user = self.User(
            username="TestUser",
            password=self.hash_password("testpass"),
            is_admin=False
        )
        self.db.session.add(user)
        self.db.session.commit()
        
        saved = self.db.session.get(self.User, user.id)
        self.assertIsNotNone(saved)
        self.assertEqual(saved.username, "TestUser")
        self.assertFalse(saved.is_admin)
```

---

## Тест CRUD операций:

```python
def test_crud_operations(self):
    """Проверка полного цикла CRUD"""
    with self.app.app_context():
        # Create
        currency = self.Currency(
            num_code="840", char_code="USD", 
            name="Доллар США", value=90.0, nominal=1
        )
        self.db.session.add(currency)
        self.db.session.commit()
        
        # Read
        saved = self.Currency.query.filter_by(char_code="USD").first()
        self.assertIsNotNone(saved)
        
        # Update
        saved.value = 91.5
        self.db.session.commit()
        updated = self.db.session.get(self.Currency, saved.id)
        self.assertEqual(updated.value, 91.5)
        
        # Delete
        self.db.session.delete(saved)
        self.db.session.commit()
        deleted = self.db.session.get(self.Currency, saved.id)
        self.assertIsNone(deleted)
```
## Тест с mock для API::

```python
@patch('app.requests.get')
def test_api_fetch_currencies(self, mock_get):
    """Тестирование получения курсов через API с mock"""
    mock_response = Mock()
    mock_response.json.return_value = {
        'Valute': {
            'USD': {'NumCode': '840', 'CharCode': 'USD', 
                   'Name': 'Доллар США', 'Value': 91.23, 'Nominal': 1}
        }
    }
    mock_get.return_value = mock_response
    
    response = mock_get('https://www.cbr-xml-daily.ru/daily_json.js')
    data = response.json()
    
    mock_get.assert_called_once_with('https://www.cbr-xml-daily.ru/daily_json.js')
    self.assertIn('Valute', data)
    self.assertEqual(data['Valute']['USD']['CharCode'], 'USD')
```
---

## ✔ Пример вывода:

![test](https://github.com/user-attachments/assets/2a56cc1f-49a3-492d-8146-1eeab2d27c05)

---

# **7. Выводы о применении MVC, работе с SQLite, обработке маршрутов и рендеринге шаблонов**

1. Применение архитектуры MVC:

В ходе работы успешно применена архитектура Model-View-Controller:

   * **Модели** — определены как классы SQLAlchemy в app.py, инкапсулируют структуру данных, валидацию и связи между сущностями. Все модели используют параметризованные запросы для защиты от SQL-инъекций.
   * **Представления** — реализованы как HTML-шаблоны Jinja2 в папке templates/. Использовано наследование шаблонов через base.html, что позволило избежать дублирования кода и обеспечить единообразие интерфейса. Шаблоны динамически отображают данные из базы с условным рендерингом для разных ролей пользователей.
   * **Контроллеры** — функции-обработчики маршрутов в app.py логически разделены по функциональности (аутентификация, управление пользователями, работа с валютами, административные функции). Каждый контроллер выполняет строго определенные задачи.

2. База данных SQLite продемонстрировала свою эффективность для учебного проекта:

* Простота развертывания — не требуется установка дополнительного ПО, база хранится в едином файле currencies.db
* Автоматическое управление — SQLAlchemy обрабатывает соединения, транзакции и ошибки
* Сохранение данных — файловая база сохраняет данные между перезапусками приложения
* Производительность — достаточная скорость работы для учебного приложения с десятками записей

3. Обработка маршрутов

Система маршрутизации Flask успешно обрабатывает все требуемые эндпоинты:

* Публичные маршруты (/, /author, /login, /register) — доступны без авторизации
* Защищенные маршруты (/users, /currencies, /user/subscriptions) — требуют аутентификации
* Административные маршруты (/admin/*) — требуют прав администратора
* Динамические маршруты (/user?id=..., /subscribe/<int:currency_id>) — поддерживают параметры

Реализована полноценная система аутентификации с хешированием паролей (SHA-256) и управлением сессиями через Flask-Session.

4. Рендеринг шаблонов

Jinja2 как шаблонизатор продемонстрировал свои преимущества:

* Наследование шаблонов — базовый шаблон содержит общую структуру, навигацию и стили
* Динамическое содержимое — шаблоны отображают данные из базы с форматированием
* Условный рендеринг — разные элементы интерфейса для обычных пользователей и администраторов
* Безопасность — автоматическое экранирование HTML для предотвращения XSS-атак

5. Достигнутые результаты

* Полная реализация CRUD — для всех сущностей (User, Currency, UserCurrency) реализованы операции создания, чтения, обновления и удаления
* Система подписок — пользователи могут подписываться на интересующие валюты и просматривать свои подписки
* Административный интерфейс — администраторы могут управлять пользователями, валютами и просматривать статистику
* Интеграция с API — возможность загрузки актуальных курсов валют с Центробанка России
* Тестирование — написаны модульные тесты, покрывающие основные функции приложения
* Безопасность — реализовано хеширование паролей, защита от SQL-инъекций, управление сессиями

7. Приложение полностью соответствует требованиям лабораторной работы и демонстрирует практическое применение MVC архитектуры, работу с реляционными базами данных и создание полнофункциональных веб-приложений на Python.
